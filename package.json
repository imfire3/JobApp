{
  "name": "jobapp",
  "version": "0.1.0",
  "description": "A minimal job board where you can post jobs and apply to them.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.19.0",
    "cheerio": "^1.2.0",
    "express": "^4.19.2",
    "xlsx": "^0.18.5"
  }
}
